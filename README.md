Nested
======

Online pedigree editor.


Development
-----------

To get started, first install all dependencies:

    npm install

Build the website:

    npm run dist

The result can be found in `dist/`.
